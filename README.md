# MiShell


## This is an effective fork of my CISC361 shell project

## How to Use:

1. Compile using `make` or `make mishell`
2. Clean using `make clean`
3. To run, execute `./mishell` with or without a file as an argument
